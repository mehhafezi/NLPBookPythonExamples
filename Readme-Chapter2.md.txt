# فصل دوم - مثال‌های پایتون

در این فصل، چند مثال ساده برای مثالهای عملی استاده از کتابخاه ها در حوزه علو انسای و اجتاعی ارائه شده‌اند.

## فایل‌ها

- `example1.py`: توکن سازی با استفاده از NTK
- `example2.py`: نرمال سازی
- 'example3.py': ریشه یابی

## نحوه اجرا

برای اجرای هر فایل، کافیست در ترمینال یا محیط پایتون دستور زیر را وارد کنید:

```bash
python example1.py
اگر از Google Colab استفاده می‌کنید، می‌توانید فایل‌ها را کپی کرده و در نوت‌بوک اجرا کنید.

برای شروع به کار لازم است ابتدا کتابخاه هایی که با آنها کار داریم را نصب کنیم. به عنون مثال در تمرین NLTK نیاز به نصب کتابخانه آن داریم که ب دستور زیر می توان آن را نصب کرد:
pip install nltk

- کتابخانه stanfordnlp نسخه پایتونی CoreNLP است، اما برای استفاده کامل از Stanford CoreNLP باید فایل‌های جاوا و مدل‌های آن را جداگانه دانلود و پیکربندی کرد. 
